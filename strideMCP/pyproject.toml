[project]
name = "stridemcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.11.0",
    "python-dotenv>=1.1.1",
]

[tool.uv]
package = true

[project.scripts]
server = "server.main:main"

